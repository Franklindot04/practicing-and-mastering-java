# Operations, Upgrades, And Rollbacks Quiz

## Multiple Choice

1. What does `helm lint` help with?
   - A. Finding common chart issues
   - B. Starting a JVM
   - C. Opening a database connection
   - D. Deleting Git branches

2. What does `helm upgrade` conceptually do?
   - A. Changes an existing release
   - B. Creates a Java class
   - C. Formats Markdown
   - D. Builds a Docker image

3. What does `helm history` show?
   - A. Release revisions
   - B. Java stack frames
   - C. SQL query plans
   - D. HTTP request headers

## Short Answer

4. List four things to inspect in rendered manifests before any cluster use.

5. Name one thing Helm rollback may not undo.

## Chart Safety

6. A learner commits a `values-prod.yaml` with a real database password. What is wrong with this?
