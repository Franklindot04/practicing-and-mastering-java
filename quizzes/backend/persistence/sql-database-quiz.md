# SQL And Database Quiz

## Multiple Choice

1. What does persistence mean?
   - A. Data disappears when the method returns.
   - B. Data survives after the program stops.
   - C. Code compiles without warnings.
   - D. The API has no errors.

2. Which SQL clause filters rows?
   - A. `WHERE`
   - B. `ORDER BY`
   - C. `LIMIT`
   - D. `INSERT`

3. What does a primary key do?
   - A. Sorts rows alphabetically.
   - B. Uniquely identifies a row.
   - C. Deletes duplicate tables.
   - D. Stores passwords.

4. Which statement needs special caution because a missing `WHERE` can change many rows?
   - A. `SELECT`
   - B. `UPDATE`
   - C. `ORDER BY`
   - D. `LIMIT`

## Short Answer

5. What is the difference between a row and a column?

6. What is a foreign key used for?

## Design Reading

7. A `comments` table has `task_id`. What relationship might this represent?
