# JPA And Spring Data Quiz

## Multiple Choice

1. What is JPA?
   - A. A Java ORM specification
   - B. A production database
   - C. A Docker image
   - D. A frontend framework

2. What is Hibernate?
   - A. A JPA implementation
   - B. A JSON response format
   - C. A Git command
   - D. A secret manager

3. Which annotation marks a JPA entity primary key?
   - A. `@Column`
   - B. `@Id`
   - C. `@Table`
   - D. `@Transactional`

4. What does `JpaRepository<TaskEntity, Long>` say?
   - A. The API only supports strings.
   - B. The entity type is `TaskEntity` and the id type is `Long`.
   - C. The controller has no service.
   - D. The database must be external.

## Short Answer

5. Why does Spring Data JPA reduce repository boilerplate?

6. What is lazy loading?

## Design Reading

7. Why might a very long derived query method name become hard to maintain?
