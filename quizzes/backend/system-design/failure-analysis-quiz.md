# Failure Analysis Quiz

1. A downstream payment boundary times out. What does the timeout fail to prove?
2. A queue backlog grows for one message type. What should you inspect?
3. A cache returns old order status. Which requirement decides whether this is acceptable?
4. A database connection pool is exhausted. Which design controls may help?
5. A circuit breaker opens. What is it protecting?
6. A retry loop has no maximum attempts. What failure can it create?
7. A notification worker sends duplicates. Which pattern helps protect side effects?
8. A canary shows increased errors. What should the deployment plan define?
