# Docker And CI/CD Quiz

## Multiple Choice

1. What is a Docker image?
   - A. A running process only
   - B. A package used to create containers
   - C. A Git branch
   - D. A Maven test report

2. What should a `.dockerignore` usually exclude?
   - A. `.env` files
   - B. Java source files in every case
   - C. The application jar in every case
   - D. README files in every case

3. What is a common CI step for Java projects?
   - A. Print all environment variables
   - B. Run `mvn test`
   - C. Commit `target/`
   - D. Store secrets in logs

## Short Answer

4. Why should deployment be separate from the first basic CI workflow?

5. Name two common CI/CD mistakes.

## Design Reading

6. A workflow deploys on every pull request and requires production credentials to run tests. What is wrong with that design?
