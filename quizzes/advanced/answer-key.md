# Advanced Java Foundation Quiz Answer Key

## Concurrency And JVM Quiz

1. B. Threads can access shared mutable data at overlapping times.
2. B. `ExecutorService` manages thread creation and task execution.
3. B. `Future.get()` waits for and returns a task result.
4. C. `ConcurrentHashMap` is designed for concurrent map updates.
5. A. The heap mainly stores objects.
6. Shutting down lets worker threads finish and allows the application to exit cleanly.
7. `synchronized` protects critical sections so only one thread at a time uses guarded state.
8. The JVM controls collection timing based on runtime conditions, so code should not depend on exact timing.
9. Measurement shows where time or memory is actually spent and prevents optimizing the wrong thing.
10. It prints `2`.
11. It prints `JAVA`.

## Design Patterns And SOLID Quiz

1. B. A design pattern names a common solution to a recurring design problem.
2. A. A factory hides construction details.
3. A. A class should have one clear reason to change.
4. A. Dependency inversion encourages high-level code to depend on abstractions.
5. A pattern can make code worse when it adds ceremony for a problem that is still simple.
6. Interfaces make code easier to replace, test, and extend.
7. Code should be open to extension but closed to unnecessary modification.
8. Separating business logic from infrastructure makes behavior easier to test without external systems.
9. Dependency inversion.
10. Observer.
