# Memory Leak Reasoning Quiz

1. A cache has no maximum size and grows forever. Why can GC not fix it?
2. Allocation rate is high but heap returns to baseline after GC. Is that necessarily a leak?
3. A `ThreadLocal` value remains in pooled threads. What risk appears?
4. What does retained size help explain?
5. Why are heap dumps sensitive?
