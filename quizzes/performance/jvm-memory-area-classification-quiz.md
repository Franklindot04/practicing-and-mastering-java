# JVM Memory Area Classification Quiz

Classify each item as heap, stack, metaspace, code cache, direct memory, or native memory.

1. A newly created `Order` object.
2. A local primitive `int` in a currently executing method.
3. Class metadata for loaded application classes.
4. JIT-compiled machine code.
5. A direct `ByteBuffer` used for I/O.
6. Memory used by many Java thread stacks.
7. A static `Map` retaining cached objects.
8. A stack frame for a recursive call.
