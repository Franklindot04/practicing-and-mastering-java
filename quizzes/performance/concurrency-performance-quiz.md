# Concurrency Performance Quiz

1. Why can increasing a thread pool worsen p99 latency?
2. What is lock granularity?
3. Why can a single synchronized cache become a bottleneck?
4. How can `LongAdder` help under high update contention?
5. What downstream limits should be checked before raising concurrency?
