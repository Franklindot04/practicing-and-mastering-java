# Vocabulary And Signal Quiz

## Short Answer

1. Define telemetry in one or two sentences.
2. Explain symptoms versus causes with a Java service example.
3. Why is the "three pillars" model useful but incomplete?
4. Name two leading indicators and two lagging indicators for a worker service.
5. Explain cardinality explosion.

## Classification

Classify each item as log, metric, trace, profile, dump, audit trail, domain signal, or health evidence:

1. `checkout.payment.failed` event with request ID and safe fields.
2. p95 request duration for `createOrder`.
3. A parent span showing a retry child span.
4. CPU allocation flame graph.
5. Thread states captured during a stuck-worker incident.
6. Record of who changed a feature flag.
7. Count of reports completed late.
8. Readiness response showing dependency degraded.

