# Java Code Reading Quiz

1. Read a retry loop that catches `InterruptedException` and ignores it. What is wrong?
2. Read a map-based idempotency check using containsKey then put. What race exists?
3. Read a bulkhead without finally release. What failure appears over time?
