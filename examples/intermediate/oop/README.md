# OOP Examples

Compile and run one example at a time:

```bash
javac examples/intermediate/oop/BankAccountDemo.java
java -cp examples/intermediate/oop BankAccountDemo
```

The examples demonstrate encapsulation, interfaces, polymorphism, records, and enums.
