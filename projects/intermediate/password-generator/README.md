# Password Generator

Generate random passwords from user-selected rules.

Requirements:

- Choose length.
- Include uppercase, lowercase, digits, and symbols.
- Validate options.
- Avoid printing or storing real user passwords.
