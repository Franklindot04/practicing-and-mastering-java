# Data Ownership

Each boundary has one source of truth inside the simulation. Read models such as cache and search projection are derived and may be stale. Orders store only the data needed to explain lifecycle state; payment and reservation details remain separate.
