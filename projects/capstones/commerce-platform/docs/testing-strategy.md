# Testing Strategy

Tests are deterministic and infrastructure-independent. They use fixed clocks, deterministic identifiers, in-memory boundaries, and bounded scenario execution.

The suite favors scenario-level assertions over implementation trivia so the capstone can explain architecture behavior.
