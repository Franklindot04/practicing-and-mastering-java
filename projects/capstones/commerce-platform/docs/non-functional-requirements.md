# Non-Functional Requirements

The simulation emphasizes deterministic tests, bounded execution, clear failure states, local-only execution, and honest operational evidence. It does not measure production throughput or resilience against real infrastructure failures.

Quality attributes are represented through validation, typed contracts, metrics, audit entries, dead-letter storage, cache behavior, reconciliation reports, and architecture fitness checks.
