# Security Assumptions

The simulation assumes authenticated callers and trusted workers. A deployed system would require worker identity, authorization, secret rotation, audit retention, tenant isolation, and abuse controls.
