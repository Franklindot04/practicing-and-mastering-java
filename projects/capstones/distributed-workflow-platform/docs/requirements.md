# Requirements

The platform accepts task and workflow submissions, applies idempotency keys, schedules ready tasks, leases work to registered workers, expires leases, retries safely, tracks dependencies, records durable-style state records, quarantines unsafe work, and reports operational state.
