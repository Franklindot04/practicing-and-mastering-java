# Testing Strategy

Tests use a fixed clock, deterministic identifiers, bounded data sets, no sleeps, and no external infrastructure. Assertions focus on workflow behavior, failure handling, and operational reports.
