# JDBC Mini App

Build a small database-backed Java application.

Requirements:

- Connect with JDBC.
- Use prepared statements.
- Create, read, update, and delete records.
- Keep credentials out of source control.
- Document local setup with placeholders.
