apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-ready-task-api
  namespace: learnjava-task-api
  labels:
    app: deployment-ready-task-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deployment-ready-task-api
  template:
    metadata:
      labels:
        app: deployment-ready-task-api
    spec:
      containers:
        - name: deployment-ready-task-api
          image: example/deployment-ready-task-api:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: deployment-ready-task-api-config
            - secretRef:
                name: deployment-ready-task-api-secret
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            failureThreshold: 12
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
