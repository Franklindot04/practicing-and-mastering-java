# Placeholder Secret for learning only. Do not commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm-ready-task-api.fullname" . }}-secret
  labels:
    {{- include "helm-ready-task-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  DEMO_DATABASE_PASSWORD: {{ .Values.secretExample.databasePassword | quote }}
  DEMO_JWT_SECRET: {{ .Values.secretExample.jwtSecret | quote }}
