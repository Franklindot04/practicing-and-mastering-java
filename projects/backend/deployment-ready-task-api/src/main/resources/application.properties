spring.application.name=deployment-ready-task-api
spring.profiles.default=local
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
app.demo-mode=true
app.runtime-profile=${SPRING_PROFILES_ACTIVE:local}
app.release-version=${APP_RELEASE_VERSION:0.0.0-local}
app.commit-sha=${APP_COMMIT_SHA:unknown}
