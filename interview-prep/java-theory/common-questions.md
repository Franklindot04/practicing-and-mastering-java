# Common Java Theory Questions

## Java Basics

- What is the difference between `==` and `.equals`?
- Why is Java called platform independent?
- What is the difference between primitive types and reference types?
- What happens when integer division has a remainder?

## OOP

- Explain encapsulation with an example.
- When would you use an interface instead of an abstract class?
- What is polymorphism?
- Why can inheritance be overused?

## Collections

- When would you use `ArrayList` vs `LinkedList`?
- When would you use `HashSet`, `LinkedHashSet`, or `TreeSet`?
- How does `HashMap` differ from `TreeMap`?
- Why should you avoid modifying a collection inside an enhanced `for` loop?

## Generics

- Why do generics improve type safety?
- What is a raw type?
- What does `List<?>` mean?

## Exceptions

- What is the difference between checked and unchecked exceptions?
- When should you create a custom exception?
- Why is catching `Exception` usually too broad?

## Streams

- What is the difference between `map` and `filter`?
- When is a loop clearer than a stream?
- Why should stream operations avoid shared mutable state?

## Concurrency

- What is a race condition?
- What is a deadlock?
- Why use an executor instead of creating many raw threads?

## JVM

- What is the difference between stack and heap?
- What does garbage collection do?
- Why should you profile before optimizing?
