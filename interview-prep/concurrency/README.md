# Concurrency Interview Prep

Core questions:

- What is a thread?
- What is a race condition?
- What does `synchronized` do?
- What is an executor?
- What is `CompletableFuture`?
- What are concurrent collections?

Good answers should include correctness, failure modes, and cleanup.
